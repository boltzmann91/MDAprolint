# MDAprolint
A python package that uses MDAnalysis to calculate lipid-protein residence times and continuous distances. Based on the principles of Prolint.

Install the package by running the following command:

`pip install mdaprolint`
